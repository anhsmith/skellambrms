# Workflow derived from https://github.com/r-lib/actions/tree/v2/examples
#
# TWO JOBS, ON DELIBERATELY DIFFERENT CADENCES.
#
# Everything in this package that fits a model is guarded twice: by
# skip_on_cran(), and by skip_if_no_stan() (tests/testthat/helper-stan.R), which
# tries to COMPILE a trivial Stan program. Both guards are needed and neither
# implies the other:
#
#   * r-lib/actions/check-r-package sets NOT_CRAN=true, so skip_on_cran() does
#     NOT fire unless a job overrides it. The `check` job below sets
#     NOT_CRAN: false explicitly for exactly that reason.
#   * brms imports rstan, so rstan is present even under dependencies: 'hard'.
#     skip_if_not_installed("brms") therefore does not establish that a fit can
#     run; only attempting a compile does.
#
# The first CI run of this code, when it was part of pairedcountbrms, failed
# because these families' fits skipped on a file-local stan_ready check while
# the joint bivariate-count families' fits, lacking one, reached
# rstan::stan_model() and errored. Those families are now a separate package
# (https://github.com/anhsmith/bicountbrms); the guard structure they forced is
# still the right one here.
#
# CORRECTION (pairedcountbrms 0.8.0). This file previously asserted that
# dependencies: '"hard"'
# leaves rstan unable to compile because the Boost headers (BH) are absent.
# That is FALSE, and relying on it silently turned the fast job into a slow one.
# rstan declares `LinkingTo: Rcpp, RcppEigen, BH, StanHeaders, RcppParallel`,
# and LinkingTo IS a hard dependency -- so BH and StanHeaders are installed
# alongside rstan and compilation works. With NOT_CRAN=true also set by the
# action, BOTH guards passed and the `check` job was compiling every model and
# running every fit: ~40 minutes against the ~5 claimed here.
#
# Two changes fixed it, and both are needed:
#   1. NOT_CRAN: false in the `check` job env, so skip_on_cran() fires and the
#      brm() fits skip.
#   2. stan_tests_enabled() (tests/testthat/helper-stan.R) guarding every
#      FILE-LEVEL rstan::stan_model() block. Those sit outside test_that(), so
#      no skip_*() can reach them; without this they compile regardless and
#      cost ~15 minutes on their own.
#
#   check       every push/PR. NOT_CRAN=false, so both the fits and the
#               file-level compiles skip. The fast signal: does it build, are
#               the docs consistent, do the examples run, do the analytic and
#               R-side tests pass.
#
#   check-stan  weekly, plus on demand. Sets NOT_CRAN=true so the fitting tests
#               actually run, and installs BOTH backends because the suite needs
#               both: every brm() fit here uses backend = "cmdstanr", while the
#               Stan-vs-R lpmf/lccdf comparisons go through
#               rstan::expose_stan_functions(). Dropping either leaves half the
#               suite skipping in a job whose whole purpose is to run it.
#
# NOTE: GitHub disables scheduled workflows after 60 days with no commits to the
# repository, so the weekly run stops once 60 days pass with nothing pushed --
# watch for the warning email, and use the Actions tab's "Run workflow" button
# to fire check-stan by hand at any time.

on:
  push:
    branches: [main, master]
  pull_request:
  schedule:
    # Mondays, 06:00 UTC (Monday evening in NZ).
    - cron: '0 6 * * 1'
  workflow_dispatch:

name: R-CMD-check.yaml

permissions: read-all

jobs:
  check:
    # Skip the fast job on the weekly cron -- check-stan supersedes it there.
    if: github.event_name != 'schedule'
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      # KEEPS THIS JOB FAST. check-r-package sets NOT_CRAN=true itself, which
      # un-skips every fitting test; setting it false here overrides that.
      # Do not remove without reading the note above about BH.
      NOT_CRAN: false
    steps:
      - uses: actions/checkout@v5

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      # `dependencies: '"hard"'` skips Suggests, so no Stan backend is installed
      # and nothing here tries to compile a model. testthat is named explicitly
      # because the tests need it even though it is a Suggests.
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          dependencies: '"hard"'
          extra-packages: |
            any::rcmdcheck
            any::testthat
          needs: check

      - uses: r-lib/actions/check-r-package@v2
        with:
          args: 'c("--no-manual")'
          upload-snapshots: true

  check-stan:
    # Weekly and on demand only -- an hour of Stan compilation does not belong
    # on every push.
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 150
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes
      # WHY THIS JOB EXISTS. Without it every model fit is skipped.
      NOT_CRAN: true
    steps:
      - uses: actions/checkout@v5

      - uses: r-lib/actions/setup-pandoc@v2

      # cmdstanr is not on CRAN; DESCRIPTION's Additional_repositories covers
      # pak, but the action needs telling too.
      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
          extra-repositories: https://stan-dev.r-universe.dev

      # BH is named explicitly. It is in rstan's LinkingTo, not its Imports, so
      # installing a binary rstan from RSPM does NOT bring it -- and without the
      # Boost headers rstan cannot compile a model. That is what made the first
      # run of this job pass in 68 seconds while every fit skipped.
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::rcmdcheck
            any::rstan
            any::cmdstanr
            any::BH
            any::RcppEigen
            any::StanHeaders
          needs: check

      # Compiling CmdStan takes ~10 minutes, so keep it between runs.
      - name: Cache CmdStan
        uses: actions/cache@v5
        with:
          path: ~/.cmdstan
          key: cmdstan-${{ runner.os }}-v1

      - name: Install CmdStan if not cached
        run: |
          if (is.null(cmdstanr::cmdstan_version(error_on_NA = FALSE))) {
            cmdstanr::install_cmdstan(cores = 2)
          } else {
            cat("CmdStan already present:", cmdstanr::cmdstan_version(), "\n")
          }
        shell: Rscript {0}

      # FAIL LOUDLY RATHER THAN SKIP. The fitting tests are guarded by
      # skip_if_no_stan() (tests/testthat/helper-stan.R), which is correct for a
      # machine without a toolchain but turns this job into a silent no-op if it
      # ever lacks one: every fit skips and the job reports success. This job
      # exists to run those fits, so a broken toolchain is a failure, not a skip.
      - name: Verify the Stan toolchain before checking
        run: |
          ok <- tryCatch(
            { rstan::stan_model(model_code = "model {}"); TRUE },
            error = function(e) { message(conditionMessage(e)); FALSE }
          )
          if (!ok) {
            stop("check-stan requires a working Stan toolchain; refusing to ",
                 "pass by skipping every model fit.")
          }
          cat("Stan toolchain OK\n")
        shell: Rscript {0}

      - uses: r-lib/actions/check-r-package@v2
        with:
          args: 'c("--no-manual")'
          upload-snapshots: true

      # Guard against the inverse failure: the toolchain works but the fits are
      # skipped for some other reason, and the job still passes. This package has
      # no opt-in gates left -- the calibration assessment went to bicountbrms
      # with the joint families -- so in this job nothing should skip at all.
      - name: Assert the model fits actually ran
        if: always()
        run: |
          f <- list.files("check", pattern = "testthat[.]Rout",
                          recursive = TRUE, full.names = TRUE)
          if (!length(f)) stop("no testthat output found")
          txt <- paste(readLines(f[1], warn = FALSE), collapse = "\n")
          m <- regmatches(txt, regexpr("SKIP [0-9]+", txt))
          if (!length(m)) stop("could not read a SKIP count from testthat output")
          n <- as.integer(sub("SKIP ", "", m))
          cat("skips reported:", n, "\n")
          if (n > 0) {
            stop("expected no skips but saw ", n,
                 " -- model fits are being skipped in a job meant to run them.")
          }
        shell: Rscript {0}
