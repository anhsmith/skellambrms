% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{skellam2}
\alias{skellam2}
\alias{skellam2_stanvars}
\alias{log_lik_skellam2}
\alias{posterior_predict_skellam2}
\alias{posterior_epred_skellam2}
\title{Asymmetric Skellam custom family for brms}
\usage{
skellam2()

skellam2_stanvars()

log_lik_skellam2(i, prep)

posterior_predict_skellam2(i, prep, ...)

posterior_epred_skellam2(prep)
}
\value{
\code{skellam2()} returns a brms \code{custom_family} object. \code{skellam2_stanvars()}
returns a \code{stanvars} object holding the Stan code for \code{skellam2_lpmf}.
\code{log_lik_skellam2()} returns a numeric vector of log-densities, one per
posterior draw, for observation \code{i}. \code{posterior_predict_skellam2()}
returns a vector of simulated differences, one per posterior draw, for
observation \code{i}, drawn subject to that row's \code{resp_trunc()} bounds where
it has any. \code{posterior_epred_skellam2()} returns a draws x observations
matrix of means, taken over the truncated distribution on any row that is
bounded.
}
\description{
Returns a brms custom family for the general (asymmetric) Skellam
distribution, Skellam(theta1, theta2) — the distribution of the
difference of two independent Poisson(theta1), Poisson(theta2) random
variables with possibly unequal rates. Two parameters: \code{mu} (link =
"identity"), the mean of the difference, and \code{sigmaexcess} (link =
"log", so \verb{>= 0}), from which \code{sigma}, the SD of the difference, and
the underlying rates \code{theta1}, \code{theta2} are derived as transformed
quantities (see Details).

Use in a brm() call as:
brm(y ~ ..., family = skellam2(), stanvars = skellam2_stanvars(), data = ...)
}
\details{
\strong{Naming note.} \code{brms::custom_family()} disallows underscores in
\code{dpars} (\code{stop2("Dots or underscores are not allowed in 'dpars'.")}),
so the second parameter is spelled \code{sigmaexcess}, not \code{sigma_excess}
as in the package's design notes and Stan code comments — the two
names refer to the same quantity.

\strong{Constraint algebra — corrected from the original design.} The
natural-seeming construction \code{sigma = sqrt(mu^2 + sigmaexcess^2)}
(Pythagorean in mu and sigmaexcess) only guarantees \verb{sigma >= |mu|}.
That is NOT the condition Skellam validity actually needs. With
\code{theta1 = (sigma^2 + mu) / 2} and \code{theta2 = (sigma^2 - mu) / 2}
(from \code{theta1 + theta2 = sigma^2} and \code{theta1 - theta2 = mu}),
\verb{theta1, theta2 >= 0} requires \verb{sigma^2 >= |mu|} — i.e. Var >= |mean|,
the genuine Skellam constraint (sum of two nonnegative Poisson rates
is always >= their difference's absolute value). \verb{sigma >= |mu|} and
\verb{sigma^2 >= |mu|} coincide only when \verb{|mu| >= 1}; for \verb{|mu| < 1} they
diverge, and \code{sigma = sqrt(mu^2 + sigmaexcess^2)} can produce a
\emph{negative} theta1 or theta2 — confirmed numerically, e.g.
\verb{mu = 0.5, sigmaexcess = 0} gives \code{sigma = 0.5}, \code{theta2 = -0.125}.
This package instead uses:
sigma^2 = |mu| + sigmaexcess^2
which guarantees \verb{sigma^2 >= |mu|} directly (the right-hand side is
\verb{|mu|} plus a nonnegative term), for every \code{mu} and every
\code{sigmaexcess >= 0}, with equality (the minimal-spread boundary) at
\code{sigmaexcess = 0}. \code{theta1} and \code{theta2} are then both sums of
nonnegative terms (verify: for \code{mu >= 0}, \code{theta1 = mu + sigmaexcess^2/2 >= 0} and \code{theta2 = sigmaexcess^2/2 >= 0}; for \code{mu < 0}, the roles swap) — strictly positive whenever \code{sigmaexcess > 0},
which the log link guarantees for any finite linear predictor. At
\code{mu = 0}, this reduces exactly to skellam1's symmetric family
(\code{sigma = sigmaexcess}, \code{theta1 = theta2 = sigmaexcess^2 / 2}).

\strong{Generated-quantities note.} This family does \emph{not} expose \code{mu},
\code{sigma}, \code{sigma^2}, \code{theta1}, \code{theta2} via a Stan \verb{generated quantities} block. Confirmed via \code{make_stancode()}: brms declares a
custom family's per-observation dpar vectors (\code{mu}, \code{sigmaexcess}
here) as local variables inside the generated model's \code{model} block,
not \verb{transformed parameters} — out of Stan-scope for \verb{generated quantities}, regardless of \code{loop = TRUE/FALSE}. Reconstructing them
from brms's internal linear-predictor variable names (\code{Xc}, \code{b},
\code{Intercept}, ...) would only work for simple fixed-effects-only
formulas, breaking silently for anything with random effects or
splines. \code{skellam2_dpars()} (below) reports the same five quantities
from R instead, via \code{brms::get_dpar()} — works for any formula.
}
\seealso{
\code{\link[=skellam2_lccdf_stanvars]{skellam2_lccdf_stanvars()}} for truncation; \code{\link[=skellam2_dpars]{skellam2_dpars()}} to
recover \code{sigma}, \code{theta1} and \code{theta2} from a fit; \code{\link[=skellam1]{skellam1()}} for
the fixed-mean family this one reduces to at \code{mu = 0}; \code{\link[=dlaplace2]{dlaplace2()}}
and \code{\link[=dnorm2]{dnorm2()}} for free-mean families that leave mean and spread
uncoupled.
}
\keyword{internal}
